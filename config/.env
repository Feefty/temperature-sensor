PORT=3000
NODE_ENV=development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=temperature_sensor
DB_USER=admin
DB_PASSWORD=password
# these values should be overriden by argoCD and ranhcer (env vars and secrets), at least this is what we do in Platform Team
