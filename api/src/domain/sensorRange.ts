// The sensor's physical operating range in degrees Celsius. The random sensor produces values in
// it, and thresholds must fall inside it (a bound the sensor can never reach classifies nothing).
export const SENSOR_MIN = -10;
export const SENSOR_MAX = 50;
